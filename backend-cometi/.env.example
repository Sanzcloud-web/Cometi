# OpenAI Configuration
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_MODEL=gpt-4o-mini
OPENAI_SUGGESTIONS_MODEL=gpt-4.1-nano

# CORS Configuration
ORIGIN=https://your-extension-id.chromiumapp.org

# Optional: Embeddings Database (PostgreSQL)
DB_EMBEDDING=postgresql://user:password@host/database?sslmode=require
EMBEDDING_MODEL=text-embedding-3-small
RESUME_TOP_K=6
RESUME_QUERY=RESUME
