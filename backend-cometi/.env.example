# Variables pour Vercel (copie dans le dashboard ou dans un fichier .env local)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini
ORIGIN=*  # Remplace par l'URL de ta build côté extension si tu veux verrouiller le CORS
