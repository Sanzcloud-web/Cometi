# Variables pour Vercel (copie dans le dashboard ou dans un fichier .env local)
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-4o-mini

# Origine autorisée pour les requêtes (CORS)
# Laisse * pendant le dev local (extension non empaquetée ou Vite)
# Remplace par chrome-extension://<id> une fois publiée.
ORIGIN=*

# Embeddings DB (PostgreSQL - Neon par ex.)
# Ex: DB_EMBEDDING="postgresql://<user>:<password>@<host>/<database>?sslmode=require"

# Modèle d'embedding (OpenAI)
EMBEDDING_MODEL=text-embedding-3-small

# (Optionnel) Requête de récupération et nombre de chunks
# Par défaut, on utilise la requête "RESUME" et on prend les 8 meilleurs chunks.
RESUME_QUERY=RESUME
RESUME_TOP_K=8

DB_EMBEDDING=xxxx
