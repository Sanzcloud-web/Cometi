import type { SlashCommand } from '../components/SlashCommandMenu';

export const SLASH_COMMANDS: SlashCommand[] = [
  {
    id: 'resume',
    label: '/resume',
    value: '/resume',
    description: 'Résume la page active et liste les sources.'
  },
];

